### Usage:
```
   import SecondStreetLogWriter form 'second-street-log-writer'
   
   const logWriter = new SecondStreetLogWriter(logFileSize, logFileLinesToSend, telemetryApi, logPath)
   
   logWriter.setToken(token);
   logWriter.writeLog(logObject);
```
